// small helpers
export function getTodayKey(): string {
  // DEBUG: Hardcoded to 14 Nov for testing
  return "14nov";
  
  // const now = new Date();
  // const day = now.getDate();
  // const month = now.toLocaleString("en-US", { month: "short" }).toLowerCase();
  // return `${day}${month}`;
}
